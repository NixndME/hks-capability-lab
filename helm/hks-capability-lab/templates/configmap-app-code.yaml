apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "hks.fullname" . }}-app-code
  namespace: {{ include "hks.namespace" . }}
  labels:
    {{- include "hks.labels" . | nindent 4 }}
data:
  app.py: |
{{ .Files.Get "files/app.py" | indent 4 }}
