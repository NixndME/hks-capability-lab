# Validation runners

Placeholder. Code that executes the definitions in `../definitions/*.yaml`
against a live cluster (used by both the backend API and, eventually, a
`run-hks-test.sh` mode that consumes the same definitions instead of its
current hard-coded menu) lives here. Populated alongside the backend phase.
